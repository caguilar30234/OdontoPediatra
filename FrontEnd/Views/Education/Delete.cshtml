@model FrontEnd.Models.EducationViewModel

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_LayoutAccounts.cshtml";
    }

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>EducationViewModel</h4>
    <hr />
    <dl class="row">
        @Html.HiddenFor(Model=>Model.EducationId)

        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Level)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Level)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        @Html.HiddenFor(Model=>Model.EducationId)
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-controller="Home" asp-action="Paciente" class="btn btn-primary">Regresar</a>
    </form>
</div>
